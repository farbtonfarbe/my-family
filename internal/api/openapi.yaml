openapi: 3.0.3
info:
  title: My Family Genealogy API
  description: |
    REST API for self-hosted genealogy software. Supports person and family
    management, GEDCOM import/export, pedigree visualization, and search.
  version: 1.0.0
  license:
    name: MIT

servers:
  - url: /api/v1
    description: API version 1

tags:
  - name: persons
    description: Individual person management
  - name: families
    description: Family unit management
  - name: pedigree
    description: Ancestry visualization
  - name: search
    description: Person search
  - name: browse
    description: Browse by surname and place
  - name: gedcom
    description: GEDCOM import/export
  - name: sources
    description: Source record management
  - name: citations
    description: Citation management
  - name: export
    description: Data export
  - name: history
    description: Change history and audit trail
  - name: rollback
    description: Rollback and restore point management
  - name: media
    description: Media file management (photos, documents)
  - name: quality
    description: Data quality metrics
  - name: statistics
    description: Tree-wide statistics
  - name: reports
    description: Genealogical reports (Ahnentafel, etc.)
  - name: snapshots
    description: Research milestone snapshots
  - name: relationships
    description: Relationship calculation between people
  - name: notes
    description: GEDCOM NOTE record management
  - name: submitters
    description: GEDCOM SUBM record management (file provenance)
  - name: associations
    description: GEDCOM ASSO record management (non-family relationships)
  - name: lds-ordinances
    description: LDS temple ordinance management (BAPL, CONL, ENDL, SLGC, SLGS)

paths:
  /persons:
    get:
      operationId: listPersons
      summary: List all persons
      tags: [persons]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
        - name: sort
          in: query
          schema:
            type: string
            enum: [surname, given_name, birth_date, updated_at]
            default: surname
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: asc
        - name: research_status
          in: query
          description: Filter by research status
          schema:
            type: string
            enum: [certain, probable, possible, unknown, unset]
      responses:
        '200':
          description: List of persons
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonList'
        '400':
          $ref: '#/components/responses/BadRequest'

    post:
      operationId: createPerson
      summary: Create a new person
      tags: [persons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonCreate'
      responses:
        '201':
          description: Person created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
        '400':
          $ref: '#/components/responses/BadRequest'

  /persons/{id}:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getPerson
      summary: Get a person by ID
      tags: [persons]
      responses:
        '200':
          description: Person details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonDetail'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updatePerson
      summary: Update a person
      tags: [persons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonUpdate'
      responses:
        '200':
          description: Person updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deletePerson
      summary: Delete a person
      tags: [persons]
      responses:
        '204':
          description: Person deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Person is linked to families
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /families:
    get:
      operationId: listFamilies
      summary: List all families
      tags: [families]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of families
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FamilyList'

    post:
      operationId: createFamily
      summary: Create a new family
      tags: [families]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FamilyCreate'
      responses:
        '201':
          description: Family created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Family'
        '400':
          $ref: '#/components/responses/BadRequest'

  /families/{id}:
    parameters:
      - $ref: '#/components/parameters/familyId'

    get:
      operationId: getFamily
      summary: Get a family by ID
      tags: [families]
      responses:
        '200':
          description: Family details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FamilyDetail'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateFamily
      summary: Update a family
      tags: [families]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FamilyUpdate'
      responses:
        '200':
          description: Family updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Family'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      operationId: deleteFamily
      summary: Delete a family
      tags: [families]
      responses:
        '204':
          description: Family deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /families/{id}/children:
    parameters:
      - $ref: '#/components/parameters/familyId'

    post:
      operationId: addChildToFamily
      summary: Add a child to a family
      tags: [families]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddChild'
      responses:
        '201':
          description: Child added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FamilyChild'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Child already belongs to a family or circular relationship
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /families/{id}/children/{personId}:
    parameters:
      - $ref: '#/components/parameters/familyId'
      - name: personId
        in: path
        required: true
        schema:
          type: string
          format: uuid

    delete:
      operationId: removeChildFromFamily
      summary: Remove a child from a family
      tags: [families]
      responses:
        '204':
          description: Child removed
        '404':
          $ref: '#/components/responses/NotFound'

  /families/{id}/group-sheet:
    parameters:
      - $ref: '#/components/parameters/familyId'

    get:
      operationId: getFamilyGroupSheet
      summary: Get family group sheet data
      description: |
        Returns a traditional family group sheet view showing a family unit
        with parents and children, key events (birth, marriage, death),
        and source citations. This is a standard format used by genealogists
        worldwide for documenting family units.
      tags: [families]
      responses:
        '200':
          description: Family group sheet data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FamilyGroupSheet'
        '404':
          $ref: '#/components/responses/NotFound'

  /pedigree/{id}:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getPedigree
      summary: Get ancestor pedigree for a person
      tags: [pedigree]
      parameters:
        - name: generations
          in: query
          description: Number of ancestor generations to include
          schema:
            type: integer
            minimum: 1
            maximum: 10
            default: 4
      responses:
        '200':
          description: Pedigree data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pedigree'
        '404':
          $ref: '#/components/responses/NotFound'

  /descendancy/{id}:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getDescendancy
      summary: Get descendancy tree for a person
      description: |
        Returns the descendant tree for a person, showing children, grandchildren,
        and so on, along with spouse information for each generation.
      tags: [pedigree]
      parameters:
        - name: generations
          in: query
          description: Number of descendant generations to include
          schema:
            type: integer
            minimum: 1
            maximum: 10
            default: 4
      responses:
        '200':
          description: Descendancy data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Descendancy'
        '404':
          $ref: '#/components/responses/NotFound'

  /ahnentafel/{id}:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getAhnentafel
      summary: Get Ahnentafel (ancestor table) report for a person
      description: |
        Returns an Ahnentafel report listing ancestors using the standard numbering system:
        1 = subject, 2 = father, 3 = mother, 4 = paternal grandfather, etc.
        Each ancestor's number n has father at 2n and mother at 2n+1.
      tags: [reports]
      parameters:
        - name: generations
          in: query
          description: Number of ancestor generations to include (1-10)
          schema:
            type: integer
            minimum: 1
            maximum: 10
            default: 5
        - name: format
          in: query
          description: Output format
          schema:
            type: string
            enum: [json, text]
            default: json
      responses:
        '200':
          description: Ahnentafel report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AhnentafelResponse'
            text/plain:
              schema:
                type: string
                description: Plain text formatted report
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /search:
    get:
      operationId: searchPersons
      summary: Search for persons
      tags: [search]
      parameters:
        - name: q
          in: query
          required: true
          description: Search query (name)
          schema:
            type: string
            minLength: 2
        - name: fuzzy
          in: query
          description: Enable fuzzy matching for spelling variations
          schema:
            type: boolean
            default: false
        - $ref: '#/components/parameters/limitParam'
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResults'
        '400':
          $ref: '#/components/responses/BadRequest'

  /browse/surnames:
    get:
      operationId: browseSurnames
      summary: Get surname index with counts
      description: Returns an alphabetical index of surnames with person counts
      tags: [browse]
      parameters:
        - name: letter
          in: query
          description: Filter surnames by starting letter (A-Z)
          schema:
            type: string
            pattern: '^[A-Z]$'
      responses:
        '200':
          description: Surname index or surnames list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SurnameIndexResponse'

  /browse/surnames/{surname}/persons:
    parameters:
      - name: surname
        in: path
        required: true
        description: The surname to get people for
        schema:
          type: string
    get:
      operationId: getPersonsBySurname
      summary: Get persons with a specific surname
      tags: [browse]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of persons with the surname
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonList'

  /browse/places:
    get:
      operationId: browsePlaces
      summary: Get place hierarchy with counts
      description: Returns hierarchical place data (Country > State > County > City)
      tags: [browse]
      parameters:
        - name: parent
          in: query
          description: Parent place to get children for (empty for top-level)
          schema:
            type: string
      responses:
        '200':
          description: Place hierarchy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceIndexResponse'

  /browse/places/{place}/persons:
    parameters:
      - name: place
        in: path
        required: true
        description: The place to get people for
        schema:
          type: string
    get:
      operationId: getPersonsByPlace
      summary: Get persons associated with a place
      tags: [browse]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of persons associated with the place
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonList'

  /gedcom/import:
    post:
      operationId: importGedcom
      summary: Import a GEDCOM file
      tags: [gedcom]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: GEDCOM file to import
      responses:
        '200':
          description: Import completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportResult'
        '400':
          description: Invalid GEDCOM file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /gedcom/export:
    get:
      operationId: exportGedcom
      summary: Export all data as GEDCOM
      tags: [gedcom]
      responses:
        '200':
          description: GEDCOM file
          content:
            application/x-gedcom:
              schema:
                type: string
          headers:
            Content-Disposition:
              schema:
                type: string
                example: attachment; filename="export.ged"

  /history:
    get:
      operationId: listHistory
      summary: List global change history
      tags: [history]
      parameters:
        - name: entity_type
          in: query
          description: Filter by entity type
          schema:
            type: string
            enum: [person, family, source, citation]
        - name: from
          in: query
          description: Start date/time for history (ISO 8601)
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: End date/time for history (ISO 8601)
          schema:
            type: string
            format: date-time
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: Change history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeHistoryResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /persons/{id}/history:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getPersonHistory
      summary: Get change history for a person
      tags: [history]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: Person change history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeHistoryResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /families/{id}/history:
    parameters:
      - $ref: '#/components/parameters/familyId'

    get:
      operationId: getFamilyHistory
      summary: Get change history for a family
      tags: [history]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: Family change history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeHistoryResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /sources/{id}/history:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      operationId: getSourceHistory
      summary: Get change history for a source
      tags: [history]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: Source change history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeHistoryResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  # Rollback endpoints

  /persons/{id}/restore-points:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getPersonRestorePoints
      summary: Get restore points for a person
      description: Returns a list of versions to which the person can be rolled back
      tags: [rollback]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of restore points
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestorePointsResponse'
              example:
                items:
                  - version: 3
                    timestamp: "2024-01-15T14:30:00Z"
                    action: updated
                    summary: "updated given_name, surname"
                  - version: 2
                    timestamp: "2024-01-15T10:00:00Z"
                    action: updated
                    summary: "updated birth_date"
                  - version: 1
                    timestamp: "2024-01-14T09:00:00Z"
                    action: created
                    summary: "created John Smith"
                total: 3
                has_more: false
        '404':
          $ref: '#/components/responses/NotFound'

  /persons/{id}/rollback:
    parameters:
      - $ref: '#/components/parameters/personId'

    post:
      operationId: rollbackPerson
      summary: Rollback a person to a previous version
      description: Restores the person's data to match the state at the specified version
      tags: [rollback]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RollbackRequest'
            example:
              target_version: 2
      responses:
        '200':
          description: Rollback successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollbackResponse'
              example:
                entity_id: "123e4567-e89b-12d3-a456-426614174000"
                entity_type: "Person"
                new_version: 4
                changes:
                  given_name: "John"
                  surname: "Doe"
                message: "Person rolled back successfully"
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Cannot rollback deleted entity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /families/{id}/restore-points:
    parameters:
      - $ref: '#/components/parameters/familyId'

    get:
      operationId: getFamilyRestorePoints
      summary: Get restore points for a family
      description: Returns a list of versions to which the family can be rolled back
      tags: [rollback]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of restore points
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestorePointsResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /families/{id}/rollback:
    parameters:
      - $ref: '#/components/parameters/familyId'

    post:
      operationId: rollbackFamily
      summary: Rollback a family to a previous version
      description: Restores the family's data to match the state at the specified version
      tags: [rollback]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RollbackRequest'
      responses:
        '200':
          description: Rollback successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollbackResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Cannot rollback deleted entity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /sources/{id}/restore-points:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      operationId: getSourceRestorePoints
      summary: Get restore points for a source
      description: Returns a list of versions to which the source can be rolled back
      tags: [rollback]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of restore points
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestorePointsResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /sources/{id}/rollback:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    post:
      operationId: rollbackSource
      summary: Rollback a source to a previous version
      description: Restores the source's data to match the state at the specified version
      tags: [rollback]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RollbackRequest'
      responses:
        '200':
          description: Rollback successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollbackResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Cannot rollback deleted entity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /citations/{id}/restore-points:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      operationId: getCitationRestorePoints
      summary: Get restore points for a citation
      description: Returns a list of versions to which the citation can be rolled back
      tags: [rollback]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of restore points
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestorePointsResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /citations/{id}/rollback:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    post:
      operationId: rollbackCitation
      summary: Rollback a citation to a previous version
      description: Restores the citation's data to match the state at the specified version
      tags: [rollback]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RollbackRequest'
      responses:
        '200':
          description: Rollback successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollbackResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Cannot rollback deleted entity
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  # Media endpoints
  /persons/{id}/media:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: listPersonMedia
      summary: List media attached to a person
      tags: [media]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
      responses:
        '200':
          description: List of media
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaList'
        '404':
          $ref: '#/components/responses/NotFound'

    post:
      operationId: uploadPersonMedia
      summary: Upload media to a person
      tags: [media]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - title
                - file
              properties:
                title:
                  type: string
                  description: Display title for the media
                  maxLength: 500
                description:
                  type: string
                  description: Optional description
                media_type:
                  type: string
                  enum: [photo, document, audio, video, certificate]
                  description: Category of media
                file:
                  type: string
                  format: binary
                  description: The file to upload (max 10MB)
      responses:
        '201':
          description: Media uploaded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Media'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '413':
          description: File too large (max 10MB)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /media/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Media ID

    get:
      operationId: getMedia
      summary: Get media metadata
      tags: [media]
      responses:
        '200':
          description: Media metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Media'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateMedia
      summary: Update media metadata
      tags: [media]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaUpdate'
      responses:
        '200':
          description: Media updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Media'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteMedia
      summary: Delete media
      tags: [media]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: Media deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /media/{id}/content:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Media ID

    get:
      operationId: downloadMedia
      summary: Download media file
      tags: [media]
      responses:
        '200':
          description: Media file content
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/gif:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFound'

  /media/{id}/thumbnail:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Media ID

    get:
      operationId: getMediaThumbnail
      summary: Get media thumbnail
      tags: [media]
      responses:
        '200':
          description: Thumbnail image (JPEG)
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFound'

  # Source endpoints
  /sources:
    get:
      operationId: listSources
      summary: List all sources
      tags: [sources]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
        - name: sort
          in: query
          schema:
            type: string
            enum: [title, source_type, created_at, updated_at]
            default: title
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: asc
        - name: q
          in: query
          description: Search query to filter sources
          schema:
            type: string
      responses:
        '200':
          description: List of sources
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SourceList'
        '400':
          $ref: '#/components/responses/BadRequest'

    post:
      operationId: createSource
      summary: Create a new source
      tags: [sources]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SourceCreate'
      responses:
        '201':
          description: Source created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SourceDetail'
        '400':
          $ref: '#/components/responses/BadRequest'

  /sources/search:
    get:
      operationId: searchSources
      summary: Search sources
      tags: [sources]
      parameters:
        - name: q
          in: query
          required: true
          description: Search query
          schema:
            type: string
            minLength: 2
        - $ref: '#/components/parameters/limitParam'
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SourceSearchResults'
        '400':
          $ref: '#/components/responses/BadRequest'

  /sources/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      operationId: getSource
      summary: Get a source by ID
      tags: [sources]
      responses:
        '200':
          description: Source details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SourceDetail'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateSource
      summary: Update a source
      tags: [sources]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SourceUpdate'
      responses:
        '200':
          description: Source updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Source'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteSource
      summary: Delete a source
      tags: [sources]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: Source deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /sources/{id}/citations:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      operationId: getCitationsForSource
      summary: Get citations for a source
      tags: [sources]
      responses:
        '200':
          description: List of citations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CitationList'
        '404':
          $ref: '#/components/responses/NotFound'

  # Citation endpoints
  /citations:
    post:
      operationId: createCitation
      summary: Create a new citation
      tags: [citations]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CitationCreate'
      responses:
        '201':
          description: Citation created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Citation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Source or fact owner not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /citations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid

    get:
      operationId: getCitation
      summary: Get a citation by ID
      tags: [citations]
      responses:
        '200':
          description: Citation details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Citation'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateCitation
      summary: Update a citation
      tags: [citations]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CitationUpdate'
      responses:
        '200':
          description: Citation updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Citation'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteCitation
      summary: Delete a citation
      tags: [citations]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: Citation deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /persons/{id}/citations:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getCitationsForPerson
      summary: Get citations for a person
      tags: [citations]
      responses:
        '200':
          description: List of citations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CitationList'
        '404':
          $ref: '#/components/responses/NotFound'

  /persons/{id}/names:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getPersonNames
      summary: Get all names for a person
      description: Returns all name variants for a person (birth name, married name, aliases, etc.)
      tags: [persons]
      responses:
        '200':
          description: List of person names
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonNameList'
        '404':
          $ref: '#/components/responses/NotFound'

    post:
      operationId: addPersonName
      summary: Add a name to a person
      description: |
        Add a new name variant to a person. If is_primary is true, the existing
        primary name will be demoted to non-primary.
      tags: [persons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonNameCreate'
      responses:
        '201':
          description: Name added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonName'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /persons/{id}/names/{nameId}:
    parameters:
      - $ref: '#/components/parameters/personId'
      - name: nameId
        in: path
        required: true
        schema:
          type: string
          format: uuid

    put:
      operationId: updatePersonName
      summary: Update a person's name
      description: |
        Update an existing name variant. If is_primary is set to true, the existing
        primary name will be demoted to non-primary.
      tags: [persons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonNameUpdate'
      responses:
        '200':
          description: Name updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonName'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      operationId: deletePersonName
      summary: Remove a name from a person
      description: |
        Remove a name variant from a person. Cannot delete the last remaining name
        or the primary name (must transfer primary status first).
      tags: [persons]
      responses:
        '204':
          description: Name removed
        '400':
          description: Cannot delete last name or primary name
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'

  # Export endpoints
  /export/tree:
    get:
      operationId: exportTree
      summary: Export full family tree
      description: Export the complete family tree data in a structured format
      tags: [export]
      responses:
        '200':
          description: Family tree data
          content:
            application/json:
              schema:
                type: object
                properties:
                  persons:
                    type: array
                    items:
                      $ref: '#/components/schemas/Person'
                  families:
                    type: array
                    items:
                      $ref: '#/components/schemas/Family'

  /export/persons:
    get:
      operationId: exportPersons
      summary: Export persons data
      description: Export all persons data in a structured format
      tags: [export]
      responses:
        '200':
          description: Persons data
          content:
            application/json:
              schema:
                type: object
                properties:
                  persons:
                    type: array
                    items:
                      $ref: '#/components/schemas/Person'
                  total:
                    type: integer

  /export/families:
    get:
      operationId: exportFamilies
      summary: Export families data
      description: Export all families data in a structured format
      tags: [export]
      responses:
        '200':
          description: Families data
          content:
            application/json:
              schema:
                type: object
                properties:
                  families:
                    type: array
                    items:
                      $ref: '#/components/schemas/Family'
                  total:
                    type: integer

  /export/sources:
    get:
      operationId: exportSources
      summary: Export sources data
      description: Export all sources data in a structured format
      tags: [export]
      responses:
        '200':
          description: Sources data
          content:
            application/json:
              schema:
                type: object
                properties:
                  sources:
                    type: array
                    items:
                      $ref: '#/components/schemas/Source'
                  total:
                    type: integer

  /export/events:
    get:
      operationId: exportEvents
      summary: Export events data
      description: Export all life events data in a structured format
      tags: [export]
      responses:
        '200':
          description: Events data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventsExportResponse'

  /export/attributes:
    get:
      operationId: exportAttributes
      summary: Export attributes data
      description: Export all person attributes data in a structured format
      tags: [export]
      responses:
        '200':
          description: Attributes data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttributesExportResponse'

  /export/estimate:
    get:
      operationId: getExportEstimate
      summary: Get export size estimation
      description: |
        Returns an estimate of the export file size and record counts.
        Useful for showing users expected download size and determining
        whether to use streaming progress for large exports.
      tags: [export]
      responses:
        '200':
          description: Export estimation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportEstimate'

  # Quality and Statistics endpoints
  /quality/overview:
    get:
      operationId: getQualityOverview
      summary: Get aggregate quality metrics
      description: Returns overall data quality metrics across all persons
      tags: [quality]
      responses:
        '200':
          description: Quality overview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityOverview'

  /quality/persons/{id}:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: getPersonQuality
      summary: Get quality score for a person
      description: Returns data completeness score and issues for a specific person
      tags: [quality]
      responses:
        '200':
          description: Person quality metrics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonQuality'
        '404':
          $ref: '#/components/responses/NotFound'

  /quality/report:
    get:
      operationId: getQualityReport
      summary: Get full quality report
      description: Returns comprehensive data quality metrics including completeness, coverage, and aggregated issues
      tags: [quality]
      responses:
        '200':
          description: Full quality report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityReport'

  /quality/validation:
    get:
      operationId: getValidationIssues
      summary: Get validation issues
      description: Returns date logic and reference validation issues categorized by severity
      tags: [quality]
      parameters:
        - name: severity
          in: query
          description: Filter by severity level
          schema:
            type: string
            enum: [error, warning, info]
      responses:
        '200':
          description: Validation issues
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationIssuesResponse'

  /persons/duplicates:
    get:
      operationId: getPersonsDuplicates
      summary: Find potential duplicate persons
      description: Returns pairs of persons that may be duplicates based on name, date, and place matching
      tags: [quality]
      parameters:
        - name: limit
          in: query
          description: Maximum number of duplicate pairs to return
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
        - name: offset
          in: query
          description: Number of duplicate pairs to skip
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: Potential duplicate pairs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DuplicatesResponse'

  /persons/merge:
    post:
      operationId: mergePersons
      summary: Merge two person records
      description: |
        Merges two person records, consolidating data from the merged person into the survivor.
        The merged person is deleted after the merge. All relationships (families, citations,
        alternate names, events, media) are transferred to the survivor.
      tags: [persons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MergePersonsRequest'
      responses:
        '200':
          description: Persons merged successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MergePersonsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: |
            Conflict - version mismatch, circular merge (ancestor/descendant relationship),
            or both persons are children in different families
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /persons/duplicates/{person1Id}/{person2Id}/dismiss:
    parameters:
      - name: person1Id
        in: path
        required: true
        description: First person ID in the duplicate pair
        schema:
          type: string
          format: uuid
      - name: person2Id
        in: path
        required: true
        description: Second person ID in the duplicate pair
        schema:
          type: string
          format: uuid
    post:
      operationId: dismissDuplicate
      summary: Dismiss a duplicate pair as false positive
      description: |
        Marks a duplicate pair as a false positive. The pair will not be returned
        in future duplicate detection results.
      tags: [quality]
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DismissDuplicateRequest'
      responses:
        '204':
          description: Duplicate pair dismissed
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /persons/merge/batch:
    post:
      operationId: batchMergePersons
      summary: Batch merge multiple duplicate pairs
      description: |
        Merges multiple duplicate pairs in a single batch operation.
        Each pair is processed independently; partial failures are reported
        in the response. Successfully merged pairs continue processing even
        if earlier pairs fail.
      tags: [persons]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchMergeRequest'
      responses:
        '200':
          description: Batch operation completed (check results for individual outcomes)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchMergeResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /persons/duplicates/dismiss/batch:
    post:
      operationId: batchDismissDuplicates
      summary: Batch dismiss multiple duplicate pairs
      description: |
        Dismisses multiple duplicate pairs as false positives in a single operation.
        Each pair is processed independently; partial failures are reported in the response.
      tags: [quality]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchDismissRequest'
      responses:
        '200':
          description: Batch operation completed (check results for individual outcomes)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchDismissResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /statistics:
    get:
      operationId: getStatistics
      summary: Get tree-wide statistics
      description: Returns aggregate statistics about the family tree
      tags: [statistics]
      responses:
        '200':
          description: Tree statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Statistics'

  # Snapshot endpoints

  /snapshots:
    get:
      operationId: listSnapshots
      summary: List all snapshots
      description: Returns all research milestone snapshots ordered by creation date (newest first)
      tags: [snapshots]
      responses:
        '200':
          description: List of snapshots
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SnapshotList'

    post:
      operationId: createSnapshot
      summary: Create a new snapshot
      description: Creates a snapshot capturing the current position in the event store
      tags: [snapshots]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnapshotCreate'
            example:
              name: "Pre-DNA results"
              description: "Research state before DNA test results arrived"
      responses:
        '201':
          description: Snapshot created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Snapshot'
        '400':
          $ref: '#/components/responses/BadRequest'

  /snapshots/{id}:
    parameters:
      - $ref: '#/components/parameters/snapshotId'

    get:
      operationId: getSnapshot
      summary: Get a snapshot by ID
      tags: [snapshots]
      responses:
        '200':
          description: Snapshot details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Snapshot'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      operationId: deleteSnapshot
      summary: Delete a snapshot
      description: Removes the snapshot record (events in the store remain untouched)
      tags: [snapshots]
      responses:
        '204':
          description: Snapshot deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /snapshots/{id1}/compare/{id2}:
    parameters:
      - name: id1
        in: path
        required: true
        description: First snapshot ID
        schema:
          type: string
          format: uuid
      - name: id2
        in: path
        required: true
        description: Second snapshot ID
        schema:
          type: string
          format: uuid

    get:
      operationId: compareSnapshots
      summary: Compare two snapshots
      description: Returns the list of changes (events) between two snapshot positions
      tags: [snapshots]
      responses:
        '200':
          description: Comparison result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SnapshotComparisonResult'
        '404':
          $ref: '#/components/responses/NotFound'

  /relationship/{personId1}/{personId2}:
    get:
      operationId: getRelationship
      summary: Calculate relationship between two people
      tags:
        - relationships
      parameters:
        - name: personId1
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: personId2
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Relationship calculated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RelationshipResult'
        '404':
          description: One or both persons not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  # Notes endpoints
  /notes:
    get:
      operationId: listNotes
      summary: List all notes
      description: Returns paginated list of shared GEDCOM NOTE records
      tags: [notes]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
      responses:
        '200':
          description: List of notes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NoteList'
        '400':
          $ref: '#/components/responses/BadRequest'

    post:
      operationId: createNote
      summary: Create a new note
      tags: [notes]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NoteCreate'
      responses:
        '201':
          description: Note created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '400':
          $ref: '#/components/responses/BadRequest'

  /notes/{id}:
    parameters:
      - $ref: '#/components/parameters/noteId'

    get:
      operationId: getNote
      summary: Get a note by ID
      tags: [notes]
      responses:
        '200':
          description: Note details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateNote
      summary: Update a note
      tags: [notes]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NoteUpdate'
      responses:
        '200':
          description: Note updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteNote
      summary: Delete a note
      tags: [notes]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: Note deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  # Submitters endpoints
  /submitters:
    get:
      operationId: listSubmitters
      summary: List all submitters
      description: Returns paginated list of GEDCOM SUBM (submitter) records
      tags: [submitters]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
        - name: sort
          in: query
          schema:
            type: string
            enum: [name, updated_at]
            default: updated_at
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
      responses:
        '200':
          description: List of submitters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitterList'
        '400':
          $ref: '#/components/responses/BadRequest'

    post:
      operationId: createSubmitter
      summary: Create a new submitter
      tags: [submitters]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitterCreate'
      responses:
        '201':
          description: Submitter created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Submitter'
        '400':
          $ref: '#/components/responses/BadRequest'

  /submitters/{id}:
    parameters:
      - $ref: '#/components/parameters/submitterId'

    get:
      operationId: getSubmitter
      summary: Get a submitter by ID
      tags: [submitters]
      responses:
        '200':
          description: Submitter details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Submitter'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateSubmitter
      summary: Update a submitter
      tags: [submitters]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitterUpdate'
      responses:
        '200':
          description: Submitter updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Submitter'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteSubmitter
      summary: Delete a submitter
      tags: [submitters]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: Submitter deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  # Associations endpoints
  /associations:
    get:
      operationId: listAssociations
      summary: List all associations
      description: Returns paginated list of GEDCOM ASSO (association) records
      tags: [associations]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
        - name: sort
          in: query
          schema:
            type: string
            enum: [role, updated_at]
            default: updated_at
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
      responses:
        '200':
          description: List of associations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssociationList'
        '400':
          $ref: '#/components/responses/BadRequest'

    post:
      operationId: createAssociation
      summary: Create a new association
      description: Create a GEDCOM ASSO association between two persons
      tags: [associations]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssociationCreate'
      responses:
        '201':
          description: Association created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Association'
        '400':
          $ref: '#/components/responses/BadRequest'

  /associations/{id}:
    parameters:
      - $ref: '#/components/parameters/associationId'

    get:
      operationId: getAssociation
      summary: Get an association by ID
      tags: [associations]
      responses:
        '200':
          description: Association details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Association'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateAssociation
      summary: Update an association
      tags: [associations]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssociationUpdate'
      responses:
        '200':
          description: Association updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Association'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteAssociation
      summary: Delete an association
      tags: [associations]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: Association deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /persons/{id}/associations:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: listAssociationsForPerson
      summary: List associations for a person
      description: Returns all associations where the person is either the subject or associate
      tags: [associations]
      responses:
        '200':
          description: List of associations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Association'
        '404':
          $ref: '#/components/responses/NotFound'

  # LDS Ordinances endpoints
  /lds-ordinances:
    get:
      operationId: listLDSOrdinances
      summary: List all LDS ordinances
      description: Returns paginated list of LDS temple ordinance records (BAPL, CONL, ENDL, SLGC, SLGS)
      tags: [lds-ordinances]
      parameters:
        - $ref: '#/components/parameters/limitParam'
        - $ref: '#/components/parameters/offsetParam'
        - name: sort
          in: query
          schema:
            type: string
            enum: [type, date, updated_at]
            default: updated_at
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
      responses:
        '200':
          description: List of LDS ordinances
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LDSOrdinanceList'
        '400':
          $ref: '#/components/responses/BadRequest'

    post:
      operationId: createLDSOrdinance
      summary: Create a new LDS ordinance
      tags: [lds-ordinances]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LDSOrdinanceCreate'
      responses:
        '201':
          description: LDS ordinance created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LDSOrdinance'
        '400':
          $ref: '#/components/responses/BadRequest'

  /lds-ordinances/{id}:
    parameters:
      - $ref: '#/components/parameters/ldsOrdinanceId'

    get:
      operationId: getLDSOrdinance
      summary: Get an LDS ordinance by ID
      tags: [lds-ordinances]
      responses:
        '200':
          description: LDS ordinance details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LDSOrdinance'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      operationId: updateLDSOrdinance
      summary: Update an LDS ordinance
      tags: [lds-ordinances]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LDSOrdinanceUpdate'
      responses:
        '200':
          description: LDS ordinance updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LDSOrdinance'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      operationId: deleteLDSOrdinance
      summary: Delete an LDS ordinance
      tags: [lds-ordinances]
      parameters:
        - $ref: '#/components/parameters/versionParam'
      responses:
        '204':
          description: LDS ordinance deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /persons/{id}/lds-ordinances:
    parameters:
      - $ref: '#/components/parameters/personId'

    get:
      operationId: listLDSOrdinancesForPerson
      summary: List LDS ordinances for a person
      description: Returns all individual LDS ordinances (BAPL, CONL, ENDL, SLGC) for a person
      tags: [lds-ordinances]
      responses:
        '200':
          description: List of LDS ordinances
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LDSOrdinance'
        '404':
          $ref: '#/components/responses/NotFound'

  /families/{id}/lds-ordinances:
    parameters:
      - $ref: '#/components/parameters/familyId'

    get:
      operationId: listLDSOrdinancesForFamily
      summary: List LDS ordinances for a family
      description: Returns all spouse sealing (SLGS) ordinances for a family
      tags: [lds-ordinances]
      responses:
        '200':
          description: List of LDS ordinances
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LDSOrdinance'
        '404':
          $ref: '#/components/responses/NotFound'

components:
  parameters:
    personId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    familyId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    limitParam:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    offsetParam:
      name: offset
      in: query
      schema:
        type: integer
        minimum: 0
        default: 0

    versionParam:
      name: version
      in: query
      description: Entity version for optimistic locking
      schema:
        type: integer
        format: int64

    snapshotId:
      name: id
      in: path
      required: true
      description: Snapshot UUID
      schema:
        type: string
        format: uuid

    noteId:
      name: id
      in: path
      required: true
      description: Note UUID
      schema:
        type: string
        format: uuid

    submitterId:
      name: id
      in: path
      required: true
      description: Submitter UUID
      schema:
        type: string
        format: uuid

    associationId:
      name: id
      in: path
      required: true
      description: Association UUID
      schema:
        type: string
        format: uuid

    ldsOrdinanceId:
      name: id
      in: path
      required: true
      description: LDS Ordinance UUID
      schema:
        type: string
        format: uuid

  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Conflict:
      description: Version conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          description: Error code
          example: "NOT_FOUND"
        message:
          type: string
          description: Human-readable error message
          example: "Person not found"
        details:
          type: object
          additionalProperties: true

    ExportEstimate:
      type: object
      required:
        - person_count
        - family_count
        - source_count
        - citation_count
        - event_count
        - note_count
        - total_records
        - estimated_bytes
        - is_large_export
      properties:
        person_count:
          type: integer
          description: Number of person records
          example: 150
        family_count:
          type: integer
          description: Number of family records
          example: 50
        source_count:
          type: integer
          description: Number of source records
          example: 25
        citation_count:
          type: integer
          description: Estimated number of citations (embedded in facts)
          example: 300
        event_count:
          type: integer
          description: Estimated number of events
          example: 450
        note_count:
          type: integer
          description: Number of note records
          example: 10
        total_records:
          type: integer
          description: Total number of top-level records
          example: 235
        estimated_bytes:
          type: integer
          format: int64
          description: Estimated file size in bytes
          example: 125000
        is_large_export:
          type: boolean
          description: True if export exceeds large threshold (>1000 records or >1MB)
          example: false

    ResearchStatus:
      type: string
      description: Confidence level of genealogical data per GPS standards
      enum: [certain, probable, possible, unknown]
      default: unknown
      example: "probable"

    GenDate:
      type: object
      description: Genealogical date with flexible precision
      properties:
        raw:
          type: string
          description: Original date string
          example: "ABT 1850"
        qualifier:
          type: string
          enum: [exact, abt, cal, est, bef, aft, bet, from]
          example: "abt"
        year:
          type: integer
          example: 1850
        month:
          type: integer
          minimum: 1
          maximum: 12
        day:
          type: integer
          minimum: 1
          maximum: 31
        year2:
          type: integer
          description: End year for ranges
        month2:
          type: integer
        day2:
          type: integer

    Person:
      type: object
      required: [id, given_name, surname, version]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
          maxLength: 100
        surname:
          type: string
          maxLength: 100
        gender:
          type: string
          enum: [male, female, unknown]
        birth_date:
          $ref: '#/components/schemas/GenDate'
        birth_place:
          type: string
        birth_place_latitude:
          type: string
          nullable: true
          description: Latitude in GEDCOM format (e.g., "N42.3601")
        birth_place_longitude:
          type: string
          nullable: true
          description: Longitude in GEDCOM format (e.g., "W71.0589")
        death_date:
          $ref: '#/components/schemas/GenDate'
        death_place:
          type: string
        death_place_latitude:
          type: string
          nullable: true
          description: Latitude in GEDCOM format (e.g., "N42.3601")
        death_place_longitude:
          type: string
          nullable: true
          description: Longitude in GEDCOM format (e.g., "W71.0589")
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        research_status:
          $ref: '#/components/schemas/ResearchStatus'
        version:
          type: integer
          format: int64
          description: Optimistic locking version

    PersonCreate:
      type: object
      required: [given_name, surname]
      properties:
        given_name:
          type: string
          minLength: 1
          maxLength: 100
        surname:
          type: string
          minLength: 1
          maxLength: 100
        gender:
          type: string
          enum: [male, female, unknown]
        birth_date:
          type: string
          description: GEDCOM-format date string
          example: "1 JAN 1850"
        birth_place:
          type: string
        death_date:
          type: string
          description: GEDCOM-format date string
        death_place:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        research_status:
          $ref: '#/components/schemas/ResearchStatus'

    PersonUpdate:
      type: object
      required: [version]
      properties:
        given_name:
          type: string
          minLength: 1
          maxLength: 100
        surname:
          type: string
          minLength: 1
          maxLength: 100
        gender:
          type: string
          enum: [male, female, unknown]
        birth_date:
          type: string
        birth_place:
          type: string
        death_date:
          type: string
        death_place:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        research_status:
          $ref: '#/components/schemas/ResearchStatus'
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    PersonDetail:
      allOf:
        - $ref: '#/components/schemas/Person'
        - type: object
          properties:
            names:
              type: array
              description: All name variants for the person (birth, married, aliases, etc.)
              items:
                $ref: '#/components/schemas/PersonName'
            families_as_partner:
              type: array
              items:
                $ref: '#/components/schemas/FamilySummary'
            family_as_child:
              $ref: '#/components/schemas/FamilySummary'

    PersonSummary:
      type: object
      required: [id, given_name, surname]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
        surname:
          type: string
        birth_date:
          $ref: '#/components/schemas/GenDate'
        death_date:
          $ref: '#/components/schemas/GenDate'

    PersonList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Person'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    Family:
      type: object
      required: [id, version]
      properties:
        id:
          type: string
          format: uuid
        partner1_id:
          type: string
          format: uuid
        partner2_id:
          type: string
          format: uuid
        relationship_type:
          type: string
          enum: [marriage, partnership, unknown]
        marriage_date:
          $ref: '#/components/schemas/GenDate'
        marriage_place:
          type: string
        marriage_place_latitude:
          type: string
          nullable: true
          description: Latitude in GEDCOM format (e.g., "N39.7817")
        marriage_place_longitude:
          type: string
          nullable: true
          description: Longitude in GEDCOM format (e.g., "W89.6501")
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        version:
          type: integer
          format: int64

    FamilyCreate:
      type: object
      properties:
        partner1_id:
          type: string
          format: uuid
        partner2_id:
          type: string
          format: uuid
        relationship_type:
          type: string
          enum: [marriage, partnership, unknown]
        marriage_date:
          type: string
        marriage_place:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records

    FamilyUpdate:
      type: object
      required: [version]
      properties:
        partner1_id:
          type: string
          format: uuid
        partner2_id:
          type: string
          format: uuid
        relationship_type:
          type: string
          enum: [marriage, partnership, unknown]
        marriage_date:
          type: string
        marriage_place:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        version:
          type: integer
          format: int64

    FamilyDetail:
      allOf:
        - $ref: '#/components/schemas/Family'
        - type: object
          properties:
            partner1:
              $ref: '#/components/schemas/PersonSummary'
            partner2:
              $ref: '#/components/schemas/PersonSummary'
            children:
              type: array
              items:
                $ref: '#/components/schemas/FamilyChild'

    FamilySummary:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid
        partner1_name:
          type: string
        partner2_name:
          type: string
        relationship_type:
          type: string

    FamilyChild:
      type: object
      required: [person_id, relationship_type]
      properties:
        person_id:
          type: string
          format: uuid
        person:
          $ref: '#/components/schemas/PersonSummary'
        relationship_type:
          type: string
          enum: [biological, adopted, foster]
        sequence:
          type: integer

    AddChild:
      type: object
      required: [person_id]
      properties:
        person_id:
          type: string
          format: uuid
        relationship_type:
          type: string
          enum: [biological, adopted, foster]
          default: biological
        sequence:
          type: integer

    FamilyList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/FamilyDetail'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    FamilyGroupSheet:
      type: object
      description: Traditional family group sheet showing parents, children, and key events
      required: [id]
      properties:
        id:
          type: string
          format: uuid
          description: Family ID
        husband:
          $ref: '#/components/schemas/GroupSheetPerson'
        wife:
          $ref: '#/components/schemas/GroupSheetPerson'
        marriage:
          $ref: '#/components/schemas/GroupSheetEvent'
        children:
          type: array
          items:
            $ref: '#/components/schemas/GroupSheetChild'

    GroupSheetPerson:
      type: object
      description: Person details for group sheet display
      required: [id, given_name, surname]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
        surname:
          type: string
        gender:
          type: string
          enum: [male, female, unknown]
        birth:
          $ref: '#/components/schemas/GroupSheetEvent'
        death:
          $ref: '#/components/schemas/GroupSheetEvent'
        father_name:
          type: string
          description: Name of father (if known)
        father_id:
          type: string
          format: uuid
        mother_name:
          type: string
          description: Name of mother (if known)
        mother_id:
          type: string
          format: uuid

    GroupSheetEvent:
      type: object
      description: Event details for group sheet display
      properties:
        date:
          type: string
          description: Formatted date string
        place:
          type: string
        citations:
          type: array
          items:
            $ref: '#/components/schemas/GroupSheetCitation'

    GroupSheetChild:
      type: object
      description: Child entry in family group sheet
      required: [id, given_name, surname]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
        surname:
          type: string
        gender:
          type: string
          enum: [male, female, unknown]
        relationship_type:
          type: string
          enum: [biological, adopted, foster]
        sequence:
          type: integer
          description: Birth order
        birth:
          $ref: '#/components/schemas/GroupSheetEvent'
        death:
          $ref: '#/components/schemas/GroupSheetEvent'
        spouse_name:
          type: string
          description: Name of spouse (if married)
        spouse_id:
          type: string
          format: uuid

    GroupSheetCitation:
      type: object
      description: Citation reference for group sheet
      required: [id, source_title]
      properties:
        id:
          type: string
          format: uuid
        source_id:
          type: string
          format: uuid
        source_title:
          type: string
        page:
          type: string
        detail:
          type: string
          description: Combined citation detail (page, volume, etc.)

    Pedigree:
      type: object
      required: [root]
      properties:
        root:
          $ref: '#/components/schemas/PedigreeNode'
        generations:
          type: integer
          description: Number of generations included
        total_ancestors:
          type: integer
          description: Total number of known ancestors
        max_generation:
          type: integer
          description: Maximum generation depth reached

    PedigreeNode:
      type: object
      required: [id]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
        surname:
          type: string
        birth_date:
          $ref: '#/components/schemas/GenDate'
        death_date:
          $ref: '#/components/schemas/GenDate'
        gender:
          type: string
        generation:
          type: integer
          description: Generation level (0 = subject, 1 = parents, 2 = grandparents, etc.)
        father:
          $ref: '#/components/schemas/PedigreeNode'
        mother:
          $ref: '#/components/schemas/PedigreeNode'

    Descendancy:
      type: object
      description: Descendancy tree showing descendants of a person
      required: [root]
      properties:
        root:
          $ref: '#/components/schemas/DescendancyNode'
        generations:
          type: integer
          description: Number of generations included
        total_descendants:
          type: integer
          description: Total number of descendants
        max_generation:
          type: integer
          description: Maximum generation depth reached

    DescendancyNode:
      type: object
      description: A person node in the descendancy tree
      required: [id]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
        surname:
          type: string
        birth_date:
          $ref: '#/components/schemas/GenDate'
        death_date:
          $ref: '#/components/schemas/GenDate'
        gender:
          type: string
        generation:
          type: integer
          description: Generation level (0 = root, 1 = children, 2 = grandchildren, etc.)
        spouses:
          type: array
          items:
            $ref: '#/components/schemas/SpouseInfo'
        children:
          type: array
          items:
            $ref: '#/components/schemas/DescendancyNode'

    SpouseInfo:
      type: object
      description: Spouse information in the descendancy tree
      required: [id, name]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        marriage_date:
          $ref: '#/components/schemas/GenDate'

    AhnentafelResponse:
      type: object
      description: |
        Ahnentafel (ancestor table) report using standard genealogical numbering.
        This schema is shared between backend and frontend - changes require updates to both.
      required: [subject, entries, generations, total_count, known_count]
      properties:
        subject:
          $ref: '#/components/schemas/AhnentafelSubject'
        entries:
          type: array
          items:
            $ref: '#/components/schemas/AhnentafelEntry'
          description: List of ancestors in Ahnentafel number order
        generations:
          type: integer
          description: Number of generations included in the report
          example: 4
        total_count:
          type: integer
          description: Total number of entries (2^(generations+1) - 1)
          example: 31
        known_count:
          type: integer
          description: Number of entries with known ancestors
          example: 15

    AhnentafelSubject:
      type: object
      description: The subject (root person) of the Ahnentafel report
      required: [id, given_name, surname]
      properties:
        id:
          type: string
          format: uuid
        given_name:
          type: string
        surname:
          type: string

    AhnentafelEntry:
      type: object
      description: A single entry in the Ahnentafel report
      required: [number, generation, relationship]
      properties:
        number:
          type: integer
          description: Ahnentafel number (1=subject, 2=father, 3=mother, etc.)
          minimum: 1
          example: 4
        generation:
          type: integer
          description: Generation number (0=subject, 1=parents, 2=grandparents, etc.)
          minimum: 0
          example: 2
        id:
          type: string
          format: uuid
          description: Person ID (omitted if ancestor is unknown)
        given_name:
          type: string
          description: Given name (omitted if ancestor is unknown)
        surname:
          type: string
          description: Surname (omitted if ancestor is unknown)
        gender:
          type: string
          enum: [male, female, unknown]
          description: Gender (omitted if ancestor is unknown)
        birth_date:
          $ref: '#/components/schemas/GenDate'
        birth_place:
          type: string
        death_date:
          $ref: '#/components/schemas/GenDate'
        death_place:
          type: string
        relationship:
          type: string
          description: Human-readable relationship to subject
          example: "Father's Father"

    SearchResults:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SearchResult'
        total:
          type: integer
        query:
          type: string

    SearchResult:
      allOf:
        - $ref: '#/components/schemas/PersonSummary'
        - type: object
          properties:
            score:
              type: number
              format: float
              description: Relevance score (0-1)

    ImportResult:
      type: object
      required: [success, persons_imported, families_imported]
      properties:
        success:
          type: boolean
        persons_imported:
          type: integer
        families_imported:
          type: integer
        warnings:
          type: array
          items:
            $ref: '#/components/schemas/ImportWarning'
        errors:
          type: array
          items:
            $ref: '#/components/schemas/ImportError'

    ImportWarning:
      type: object
      required: [line, message]
      properties:
        line:
          type: integer
        record:
          type: string
          description: GEDCOM @XREF@ if available
        message:
          type: string

    ImportError:
      type: object
      required: [line, message]
      properties:
        line:
          type: integer
        record:
          type: string
        message:
          type: string

    ChangeEntry:
      type: object
      required: [id, timestamp, entity_type, entity_id, action]
      properties:
        id:
          type: string
          format: uuid
        timestamp:
          type: string
          format: date-time
        entity_type:
          type: string
          enum: [person, family, source, citation]
        entity_id:
          type: string
          format: uuid
        entity_name:
          type: string
          description: Human-readable name of the entity
        action:
          type: string
          enum: [created, updated, deleted]
        changes:
          type: object
          description: Field-level changes for updates
          additionalProperties:
            $ref: '#/components/schemas/FieldChange'
        user_id:
          type: string
          description: ID of user who made the change (null if single-user)

    FieldChange:
      type: object
      properties:
        old_value:
          description: Previous value (null for new fields)
        new_value:
          description: New value (null for removed fields)

    ChangeHistoryResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ChangeEntry'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean

    RestorePoint:
      type: object
      required: [version, timestamp, action, summary]
      properties:
        version:
          type: integer
          format: int64
          description: The version number of this restore point
          example: 2
        timestamp:
          type: string
          format: date-time
          description: When this version was created
          example: "2024-01-15T10:30:00Z"
        action:
          type: string
          enum: [created, updated, deleted, linked, unlinked]
          description: The action that created this version
          example: "updated"
        summary:
          type: string
          description: Human-readable summary of the changes
          example: "updated given_name, birth_date"

    RestorePointsResponse:
      type: object
      required: [items, total, has_more]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RestorePoint'
        total:
          type: integer
          description: Total number of restore points available
        has_more:
          type: boolean
          description: Whether there are more restore points beyond the current page

    RollbackRequest:
      type: object
      required: [target_version]
      properties:
        target_version:
          type: integer
          format: int64
          minimum: 1
          description: The version number to rollback to
          example: 2

    RollbackResponse:
      type: object
      required: [entity_id, entity_type, new_version, changes, message]
      properties:
        entity_id:
          type: string
          format: uuid
          description: ID of the entity that was rolled back
        entity_type:
          type: string
          enum: [Person, Family, Source, Citation]
          description: Type of entity that was rolled back
        new_version:
          type: integer
          format: int64
          description: The new version number after the rollback
        changes:
          type: object
          additionalProperties: true
          description: Map of field names to their restored values
        message:
          type: string
          description: Success message
          example: "Person rolled back successfully"

    # Media schemas
    Media:
      type: object
      required: [id, entity_type, entity_id, title, mime_type, filename, file_size, version]
      properties:
        id:
          type: string
          format: uuid
        entity_type:
          type: string
          enum: [person, family, source]
        entity_id:
          type: string
          format: uuid
        title:
          type: string
          maxLength: 500
        description:
          type: string
        mime_type:
          type: string
          example: "image/jpeg"
        media_type:
          type: string
          enum: [photo, document, audio, video, certificate]
        filename:
          type: string
        file_size:
          type: integer
          format: int64
          description: File size in bytes
        has_thumbnail:
          type: boolean
          description: Whether a thumbnail is available
        crop_left:
          type: integer
        crop_top:
          type: integer
        crop_width:
          type: integer
        crop_height:
          type: integer
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        # GEDCOM 7.0 enhanced fields
        files:
          type: array
          description: Multiple file references (GEDCOM 7.0 support)
          items:
            $ref: '#/components/schemas/MediaFile'
        format:
          type: string
          description: Primary format/MIME type (FORM tag)
          example: "image/jpeg"
        translations:
          type: array
          description: Translated titles (GEDCOM 7.0 support)
          items:
            type: string

    MediaFile:
      type: object
      description: A single file reference within a media object (GEDCOM 7.0 FILE structure)
      properties:
        path:
          type: string
          description: File path or URL
        format:
          type: string
          description: MIME type (FORM tag)
          example: "image/jpeg"
        media_type:
          type: string
          description: Media type categorization (MEDI tag)
          example: "PHOTO"
        title:
          type: string
          description: Title for this specific file
        translations:
          type: array
          description: Alternate versions (transcripts, thumbnails, different formats)
          items:
            $ref: '#/components/schemas/MediaTranslation'

    MediaTranslation:
      type: object
      description: Alternate version of a file (GEDCOM 7.0 FILE-TRAN structure)
      properties:
        path:
          type: string
          description: File path or URL of the translation
        format:
          type: string
          description: MIME type of the translation file

    MediaList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Media'
        total:
          type: integer
          description: Total number of media items

    MediaUpdate:
      type: object
      required: [version]
      properties:
        title:
          type: string
          maxLength: 500
        description:
          type: string
        media_type:
          type: string
          enum: [photo, document, audio, video, certificate]
        crop_left:
          type: integer
        crop_top:
          type: integer
        crop_width:
          type: integer
        crop_height:
          type: integer
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    # Browse schemas
    SurnameIndexResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SurnameEntry'
        total:
          type: integer
          description: Total number of unique surnames
        letter_counts:
          type: array
          items:
            $ref: '#/components/schemas/LetterCount'
          description: Count of surnames by starting letter

    SurnameEntry:
      type: object
      required: [surname, count]
      properties:
        surname:
          type: string
        count:
          type: integer
          description: Number of persons with this surname

    LetterCount:
      type: object
      required: [letter, count]
      properties:
        letter:
          type: string
          description: A-Z letter
        count:
          type: integer
          description: Number of surnames starting with this letter

    PlaceIndexResponse:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PlaceEntry'
        total:
          type: integer
          description: Total number of unique places at this level
        breadcrumb:
          type: array
          items:
            type: string
          description: Path from root to current level

    PlaceEntry:
      type: object
      required: [name, count]
      properties:
        name:
          type: string
          description: Place name at this level
        full_name:
          type: string
          description: Full place path
        count:
          type: integer
          description: Number of persons associated with this place
        has_children:
          type: boolean
          description: Whether this place has sub-locations

    # Source schemas
    Source:
      type: object
      required: [id, source_type, title, version]
      properties:
        id:
          type: string
          format: uuid
        source_type:
          type: string
          description: Type of source (e.g., vital_record, census, newspaper)
        title:
          type: string
        author:
          type: string
        publisher:
          type: string
        publish_date:
          type: string
        url:
          type: string
          format: uri
        repository_name:
          type: string
        collection_name:
          type: string
        call_number:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        citation_count:
          type: integer
          description: Number of citations referencing this source
        version:
          type: integer
          format: int64
          description: Optimistic locking version

    SourceCreate:
      type: object
      required: [source_type, title]
      properties:
        source_type:
          type: string
          description: Type of source (e.g., vital_record, census, newspaper)
        title:
          type: string
        author:
          type: string
        publisher:
          type: string
        publish_date:
          type: string
        url:
          type: string
          format: uri
        repository_name:
          type: string
        collection_name:
          type: string
        call_number:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records

    SourceUpdate:
      type: object
      required: [version]
      properties:
        source_type:
          type: string
        title:
          type: string
        author:
          type: string
        publisher:
          type: string
        publish_date:
          type: string
        url:
          type: string
          format: uri
        repository_name:
          type: string
        collection_name:
          type: string
        call_number:
          type: string
        notes:
          type: string
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: IDs of linked Note records
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    SourceDetail:
      allOf:
        - $ref: '#/components/schemas/Source'
        - type: object
          properties:
            citations:
              type: array
              items:
                $ref: '#/components/schemas/Citation'

    SourceList:
      type: object
      required: [sources, total]
      properties:
        sources:
          type: array
          items:
            $ref: '#/components/schemas/Source'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    SourceSearchResults:
      type: object
      required: [sources, total, query]
      properties:
        sources:
          type: array
          items:
            $ref: '#/components/schemas/Source'
        total:
          type: integer
        query:
          type: string

    # PersonName schemas
    PersonName:
      type: object
      required: [id, person_id, given_name, surname, name_type, is_primary]
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        given_name:
          type: string
          maxLength: 100
        surname:
          type: string
          maxLength: 100
        full_name:
          type: string
          readOnly: true
        name_prefix:
          type: string
        name_suffix:
          type: string
        surname_prefix:
          type: string
        nickname:
          type: string
        name_type:
          type: string
          enum: [birth, married, aka, immigrant, religious, professional]
        is_primary:
          type: boolean

    PersonNameCreate:
      type: object
      required: [given_name, surname, name_type]
      properties:
        given_name:
          type: string
          minLength: 1
          maxLength: 100
        surname:
          type: string
          maxLength: 100
        name_prefix:
          type: string
        name_suffix:
          type: string
        surname_prefix:
          type: string
        nickname:
          type: string
        name_type:
          type: string
          enum: [birth, married, aka, immigrant, religious, professional]
        is_primary:
          type: boolean
          default: false

    PersonNameUpdate:
      type: object
      properties:
        given_name:
          type: string
          minLength: 1
          maxLength: 100
        surname:
          type: string
          maxLength: 100
        name_prefix:
          type: string
        name_suffix:
          type: string
        surname_prefix:
          type: string
        nickname:
          type: string
        name_type:
          type: string
          enum: [birth, married, aka, immigrant, religious, professional]
        is_primary:
          type: boolean

    PersonNameList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PersonName'
        total:
          type: integer

    # Citation schemas
    Citation:
      type: object
      required: [id, source_id, source_title, fact_type, fact_owner_id, version]
      properties:
        id:
          type: string
          format: uuid
        source_id:
          type: string
          format: uuid
        source_title:
          type: string
          description: Title of the referenced source
        fact_type:
          type: string
          description: Type of fact being cited (e.g., birth, death, marriage)
        fact_owner_id:
          type: string
          format: uuid
          description: ID of the person or family this citation applies to
        page:
          type: string
        volume:
          type: string
        source_quality:
          type: string
          description: Quality assessment of the source
        informant_type:
          type: string
          description: Type of informant (e.g., primary, secondary)
        evidence_type:
          type: string
          description: Type of evidence (e.g., direct, indirect)
        quoted_text:
          type: string
          description: Exact text quoted from the source
        analysis:
          type: string
          description: Researcher's analysis of the citation
        template_id:
          type: string
          description: Citation template ID
        version:
          type: integer
          format: int64
          description: Optimistic locking version

    CitationCreate:
      type: object
      required: [source_id, fact_type, fact_owner_id]
      properties:
        source_id:
          type: string
          format: uuid
        fact_type:
          type: string
          description: Type of fact being cited (e.g., birth, death, marriage)
        fact_owner_id:
          type: string
          format: uuid
          description: ID of the person or family this citation applies to
        page:
          type: string
        volume:
          type: string
        source_quality:
          type: string
        informant_type:
          type: string
        evidence_type:
          type: string
        quoted_text:
          type: string
        analysis:
          type: string
        template_id:
          type: string

    CitationUpdate:
      type: object
      required: [version]
      properties:
        page:
          type: string
        volume:
          type: string
        source_quality:
          type: string
        informant_type:
          type: string
        evidence_type:
          type: string
        quoted_text:
          type: string
        analysis:
          type: string
        template_id:
          type: string
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    CitationList:
      type: object
      required: [citations, total]
      properties:
        citations:
          type: array
          items:
            $ref: '#/components/schemas/Citation'
        total:
          type: integer

    # Quality and Statistics schemas
    QualityOverview:
      type: object
      required: [total_persons, average_completeness, records_with_issues, top_issues]
      properties:
        total_persons:
          type: integer
          description: Total number of persons in the tree
        average_completeness:
          type: number
          format: float
          minimum: 0
          maximum: 100
          description: Average completeness score across all persons (0-100)
        records_with_issues:
          type: integer
          description: Number of persons with at least one data quality issue
        top_issues:
          type: array
          items:
            $ref: '#/components/schemas/QualityIssue'
          description: Most common data quality issues

    QualityIssue:
      type: object
      required: [issue, count]
      properties:
        issue:
          type: string
          description: Description of the issue
        count:
          type: integer
          description: Number of records with this issue

    PersonQuality:
      type: object
      required: [person_id, completeness_score, issues, suggestions]
      properties:
        person_id:
          type: string
          format: uuid
        completeness_score:
          type: number
          format: float
          minimum: 0
          maximum: 100
          description: Data completeness score (0-100)
        issues:
          type: array
          items:
            type: string
          description: List of data quality issues
        suggestions:
          type: array
          items:
            type: string
          description: Suggested improvements

    QualityReport:
      type: object
      description: Comprehensive data quality report with coverage metrics and issue aggregation
      required: [total_individuals, total_families, total_sources, birth_date_coverage, death_date_coverage, source_coverage, error_count, warning_count, info_count, top_issues]
      properties:
        total_individuals:
          type: integer
          description: Total number of individuals in the tree
        total_families:
          type: integer
          description: Total number of family units
        total_sources:
          type: integer
          description: Total number of source records
        birth_date_coverage:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: Fraction of individuals with birth dates (0.0-1.0)
        death_date_coverage:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: Fraction of individuals with death dates (0.0-1.0)
        source_coverage:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: Fraction of individuals with at least one source citation (0.0-1.0)
        error_count:
          type: integer
          description: Total number of error-level issues
        warning_count:
          type: integer
          description: Total number of warning-level issues
        info_count:
          type: integer
          description: Total number of info-level issues
        top_issues:
          type: array
          items:
            $ref: '#/components/schemas/QualityReportIssue'
          description: Most common issues by count

    QualityReportIssue:
      type: object
      description: Aggregated issue count for quality reports
      required: [code, count]
      properties:
        code:
          type: string
          description: Issue code identifier
        count:
          type: integer
          description: Number of occurrences of this issue

    DuplicatePair:
      type: object
      description: A pair of potentially duplicate persons
      required: [person1_id, person1_name, person2_id, person2_name, confidence, match_reasons]
      properties:
        person1_id:
          type: string
          format: uuid
          description: ID of the first person
        person1_name:
          type: string
          description: Display name of the first person
        person2_id:
          type: string
          format: uuid
          description: ID of the second person
        person2_name:
          type: string
          description: Display name of the second person
        confidence:
          type: number
          format: float
          minimum: 0
          maximum: 1
          description: Confidence score that these are duplicates (0.0-1.0)
        match_reasons:
          type: array
          items:
            type: string
          description: Reasons why these persons are considered potential duplicates

    DuplicatesResponse:
      type: object
      description: Response containing potential duplicate pairs
      required: [duplicates, total]
      properties:
        duplicates:
          type: array
          items:
            $ref: '#/components/schemas/DuplicatePair'
          description: List of potential duplicate pairs
        total:
          type: integer
          description: Total number of potential duplicate pairs found

    MergePersonsRequest:
      type: object
      description: Request to merge two person records
      required: [survivor_id, merged_id, survivor_version, merged_version]
      properties:
        survivor_id:
          type: string
          format: uuid
          description: ID of the person that will remain after merge
        merged_id:
          type: string
          format: uuid
          description: ID of the person to be merged into survivor
        survivor_version:
          type: integer
          format: int64
          description: Expected version of survivor for optimistic locking
        merged_version:
          type: integer
          format: int64
          description: Expected version of merged person for optimistic locking
        field_resolution:
          type: object
          additionalProperties:
            type: string
            enum: [survivor, merged]
          description: |
            Optional per-field source selection. Keys are field names
            (given_name, surname, gender, birth_date, birth_place,
            death_date, death_place, notes, research_status).
            Default is "survivor" for all fields unless survivor's value is empty.
          example:
            birth_date: merged
            death_date: survivor

    MergePersonsResponse:
      type: object
      description: Result of merging two persons
      required: [person, merge_summary]
      properties:
        person:
          $ref: '#/components/schemas/Person'
          description: The survivor person after merge
        merge_summary:
          $ref: '#/components/schemas/MergeSummary'

    MergeSummary:
      type: object
      description: Summary of what was merged
      required: [merged_person_name, fields_updated, families_updated, citations_transferred, names_transferred, events_transferred, media_transferred]
      properties:
        merged_person_name:
          type: string
          description: Full name of the person that was merged
        fields_updated:
          type: array
          items:
            type: string
          description: List of fields that were updated from merged person
        families_updated:
          type: integer
          description: Number of families where partner reference was updated
        citations_transferred:
          type: integer
          description: Number of citations transferred to survivor
        names_transferred:
          type: integer
          description: Number of alternate names added to survivor
        events_transferred:
          type: integer
          description: Number of life events transferred
        media_transferred:
          type: integer
          description: Number of media files transferred

    DismissDuplicateRequest:
      type: object
      description: Request to dismiss a duplicate pair as false positive
      properties:
        reason:
          type: string
          description: Optional reason for dismissal

    BatchMergeRequest:
      type: object
      description: Request to merge multiple duplicate pairs
      required: [merges]
      properties:
        merges:
          type: array
          items:
            $ref: '#/components/schemas/MergePersonsRequest'
          description: List of merge operations to perform
          minItems: 1
          maxItems: 100

    BatchMergeResponse:
      type: object
      description: Results of batch merge operation
      required: [total, successful, failed, results]
      properties:
        total:
          type: integer
          description: Total number of merge operations attempted
        successful:
          type: integer
          description: Number of successful merges
        failed:
          type: integer
          description: Number of failed merges
        results:
          type: array
          items:
            $ref: '#/components/schemas/BatchMergeResult'
          description: Individual results for each merge operation

    BatchMergeResult:
      type: object
      description: Result of a single merge operation in batch
      required: [survivor_id, merged_id, success]
      properties:
        survivor_id:
          type: string
          format: uuid
          description: ID of the survivor person
        merged_id:
          type: string
          format: uuid
          description: ID of the merged person
        success:
          type: boolean
          description: Whether the merge succeeded
        error:
          type: string
          description: Error message if merge failed
        merge_summary:
          $ref: '#/components/schemas/MergeSummary'

    BatchDismissRequest:
      type: object
      description: Request to dismiss multiple duplicate pairs
      required: [dismissals]
      properties:
        dismissals:
          type: array
          items:
            $ref: '#/components/schemas/DuplicatePairRef'
          description: List of duplicate pairs to dismiss
          minItems: 1
          maxItems: 100

    DuplicatePairRef:
      type: object
      description: Reference to a duplicate pair
      required: [person1_id, person2_id]
      properties:
        person1_id:
          type: string
          format: uuid
          description: First person ID in the pair
        person2_id:
          type: string
          format: uuid
          description: Second person ID in the pair
        reason:
          type: string
          description: Optional reason for dismissal

    BatchDismissResponse:
      type: object
      description: Results of batch dismiss operation
      required: [total, successful, failed, results]
      properties:
        total:
          type: integer
          description: Total number of dismiss operations attempted
        successful:
          type: integer
          description: Number of successful dismissals
        failed:
          type: integer
          description: Number of failed dismissals
        results:
          type: array
          items:
            $ref: '#/components/schemas/BatchDismissResult'
          description: Individual results for each dismiss operation

    BatchDismissResult:
      type: object
      description: Result of a single dismiss operation in batch
      required: [person1_id, person2_id, success]
      properties:
        person1_id:
          type: string
          format: uuid
          description: First person ID in the pair
        person2_id:
          type: string
          format: uuid
          description: Second person ID in the pair
        success:
          type: boolean
          description: Whether the dismissal succeeded
        error:
          type: string
          description: Error message if dismissal failed

    ValidationIssue:
      type: object
      description: A single validation issue detected in the data
      required: [severity, code, message]
      properties:
        severity:
          type: string
          enum: [error, warning, info]
          description: Severity level of the issue
        code:
          type: string
          description: Issue code identifier
        message:
          type: string
          description: Human-readable description of the issue
        record_id:
          type: string
          format: uuid
          description: ID of the primary record with the issue
        related_record_id:
          type: string
          format: uuid
          description: ID of a related record (if applicable)

    ValidationIssuesResponse:
      type: object
      description: Response containing validation issues categorized by severity
      required: [issues, error_count, warning_count, info_count]
      properties:
        issues:
          type: array
          items:
            $ref: '#/components/schemas/ValidationIssue'
          description: List of validation issues
        error_count:
          type: integer
          description: Total number of error-level issues
        warning_count:
          type: integer
          description: Total number of warning-level issues
        info_count:
          type: integer
          description: Total number of info-level issues

    Statistics:
      type: object
      required: [total_persons, total_families, date_range, top_surnames, gender_distribution]
      properties:
        total_persons:
          type: integer
          description: Total number of persons in the tree
        total_families:
          type: integer
          description: Total number of family units
        date_range:
          $ref: '#/components/schemas/DateRange'
        top_surnames:
          type: array
          items:
            $ref: '#/components/schemas/SurnameCount'
          description: Most common surnames (top 10)
        gender_distribution:
          $ref: '#/components/schemas/GenderDistribution'

    DateRange:
      type: object
      properties:
        earliest_birth:
          type: string
          description: Earliest birth year in the tree
        latest_birth:
          type: string
          description: Latest birth year in the tree

    SurnameCount:
      type: object
      required: [surname, count]
      properties:
        surname:
          type: string
        count:
          type: integer

    GenderDistribution:
      type: object
      required: [male, female, unknown]
      properties:
        male:
          type: integer
        female:
          type: integer
        unknown:
          type: integer

    Snapshot:
      type: object
      required: [id, name, position, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          maxLength: 100
          description: Name of the research milestone
          example: "Pre-DNA results"
        description:
          type: string
          maxLength: 500
          description: Optional description of the snapshot
          example: "Research state before DNA test results arrived"
        position:
          type: integer
          format: int64
          description: Event store position captured by this snapshot
          example: 42
        created_at:
          type: string
          format: date-time
          description: When the snapshot was created

    SnapshotCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
          description: Name of the research milestone
        description:
          type: string
          maxLength: 500
          description: Optional description of the snapshot

    SnapshotList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Snapshot'
        total:
          type: integer
          description: Total number of snapshots

    SnapshotComparisonResult:
      type: object
      required: [snapshot1, snapshot2, changes, total_count, has_more, older_first]
      properties:
        snapshot1:
          $ref: '#/components/schemas/Snapshot'
        snapshot2:
          $ref: '#/components/schemas/Snapshot'
        changes:
          type: array
          items:
            $ref: '#/components/schemas/ChangeEntry'
          description: Events that occurred between the two snapshot positions
        total_count:
          type: integer
          description: Number of changes between snapshots
        has_more:
          type: boolean
          description: Whether there are more changes beyond the limit
        older_first:
          type: boolean
          description: True if snapshot1 is the older (lower position) snapshot

    RelationshipPath:
      type: object
      properties:
        name:
          type: string
          description: Relationship name (e.g., "1st cousin once removed")
        pathFromA:
          type: array
          items:
            type: string
            format: uuid
        pathFromB:
          type: array
          items:
            type: string
            format: uuid
        commonAncestorId:
          type: string
          format: uuid
        generationDistanceA:
          type: integer
        generationDistanceB:
          type: integer

    RelationshipResult:
      type: object
      properties:
        personA:
          $ref: '#/components/schemas/Person'
        personB:
          $ref: '#/components/schemas/Person'
        paths:
          type: array
          items:
            $ref: '#/components/schemas/RelationshipPath'
        isRelated:
          type: boolean
        summary:
          type: string
          description: Human-readable relationship summary

    # Note schemas
    Note:
      type: object
      description: A shared GEDCOM NOTE record that can be referenced by multiple entities
      required: [id, text, version]
      properties:
        id:
          type: string
          format: uuid
        text:
          type: string
          description: Full note text (may contain embedded newlines from CONT/CONC lines)
        gedcom_xref:
          type: string
          description: GEDCOM cross-reference ID (e.g., "@N1@") for round-trip support
        version:
          type: integer
          format: int64
          description: Optimistic locking version
        updated_at:
          type: string
          format: date-time

    NoteCreate:
      type: object
      required: [text]
      properties:
        text:
          type: string
          description: Note text content
          minLength: 1
        gedcom_xref:
          type: string
          description: Optional GEDCOM cross-reference ID for import

    NoteUpdate:
      type: object
      required: [version]
      properties:
        text:
          type: string
          description: Updated note text
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    NoteList:
      type: object
      required: [notes, total]
      properties:
        notes:
          type: array
          items:
            $ref: '#/components/schemas/Note'
        total:
          type: integer
          description: Total number of notes
        limit:
          type: integer
        offset:
          type: integer

    # Address schema for embedded addresses
    Address:
      type: object
      description: Structured GEDCOM address (embedded in other entities)
      properties:
        line1:
          type: string
          description: First address line (ADR1)
        line2:
          type: string
          description: Second address line (ADR2)
        line3:
          type: string
          description: Third address line (ADR3)
        city:
          type: string
          description: City (CITY)
        state:
          type: string
          description: State/Province (STAE)
        postal_code:
          type: string
          description: Postal/ZIP code (POST)
        country:
          type: string
          description: Country (CTRY)
        phone:
          type: string
          description: Phone number (PHON)
        email:
          type: string
          description: Email address (EMAIL)
        fax:
          type: string
          description: Fax number (FAX)
        website:
          type: string
          description: Website URL (WWW)

    # Submitter schemas
    Submitter:
      type: object
      description: A GEDCOM SUBM (submitter) record tracking who created or submitted data
      required: [id, name, version]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Submitter's name
        address:
          $ref: '#/components/schemas/Address'
        phone:
          type: array
          items:
            type: string
          description: Phone numbers
        email:
          type: array
          items:
            type: string
          description: Email addresses
        language:
          type: string
          description: Preferred language
        media_id:
          type: string
          format: uuid
          description: ID of linked media (submitter photo)
        gedcom_xref:
          type: string
          description: GEDCOM cross-reference ID for round-trip support
        version:
          type: integer
          format: int64
          description: Optimistic locking version
        updated_at:
          type: string
          format: date-time

    SubmitterCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Submitter's name
          minLength: 1
          maxLength: 200
        address:
          $ref: '#/components/schemas/Address'
        phone:
          type: array
          items:
            type: string
          description: Phone numbers
        email:
          type: array
          items:
            type: string
          description: Email addresses
        language:
          type: string
          description: Preferred language
        media_id:
          type: string
          format: uuid
          description: ID of linked media (submitter photo)
        gedcom_xref:
          type: string
          description: Optional GEDCOM cross-reference ID for import

    SubmitterUpdate:
      type: object
      required: [version]
      properties:
        name:
          type: string
          description: Updated submitter name
          minLength: 1
          maxLength: 200
        address:
          $ref: '#/components/schemas/Address'
        phone:
          type: array
          items:
            type: string
          description: Updated phone numbers
        email:
          type: array
          items:
            type: string
          description: Updated email addresses
        language:
          type: string
          description: Updated preferred language
        media_id:
          type: string
          format: uuid
          description: Updated media ID
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    SubmitterList:
      type: object
      required: [submitters, total]
      properties:
        submitters:
          type: array
          items:
            $ref: '#/components/schemas/Submitter'
        total:
          type: integer
          description: Total number of submitters
        limit:
          type: integer
        offset:
          type: integer

    # Association schemas (GEDCOM ASSO records)
    Association:
      type: object
      required: [id, person_id, associate_id, role, version]
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
          description: The person who has the association
        person_name:
          type: string
          description: Denormalized name of the person
        associate_id:
          type: string
          format: uuid
          description: The associated person
        associate_name:
          type: string
          description: Denormalized name of the associate
        role:
          type: string
          description: Role of the association (e.g., godparent, witness)
        phrase:
          type: string
          description: Human-readable description (GEDCOM 7.0 PHRASE)
        notes:
          type: string
          description: Inline note text
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: References to Note entities
        gedcom_xref:
          type: string
          description: Original GEDCOM cross-reference ID
        version:
          type: integer
          format: int64
          description: Version for optimistic locking
        updated_at:
          type: string
          format: date-time

    AssociationCreate:
      type: object
      required: [person_id, associate_id, role]
      properties:
        person_id:
          type: string
          format: uuid
          description: The person who has the association
        associate_id:
          type: string
          format: uuid
          description: The associated person
        role:
          type: string
          description: Role of the association (e.g., godparent, witness)
        phrase:
          type: string
          description: Human-readable description (GEDCOM 7.0 PHRASE)
        notes:
          type: string
          description: Inline note text
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: References to Note entities

    AssociationUpdate:
      type: object
      required: [version]
      properties:
        role:
          type: string
          description: Updated role
        phrase:
          type: string
          description: Updated phrase
        notes:
          type: string
          description: Updated notes
        note_ids:
          type: array
          items:
            type: string
            format: uuid
          description: Updated note references
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    AssociationList:
      type: object
      required: [associations, total]
      properties:
        associations:
          type: array
          items:
            $ref: '#/components/schemas/Association'
        total:
          type: integer
          description: Total number of associations
        limit:
          type: integer
        offset:
          type: integer

    # LDS Ordinance schemas
    LDSOrdinanceType:
      type: string
      enum: [BAPL, CONL, ENDL, SLGC, SLGS]
      description: |
        LDS temple ordinance type:
        - BAPL: Baptism (LDS)
        - CONL: Confirmation (LDS)
        - ENDL: Endowment
        - SLGC: Sealing to Parents
        - SLGS: Sealing to Spouse (family level)

    LDSOrdinance:
      type: object
      required: [id, type, type_label, version, updated_at]
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: '#/components/schemas/LDSOrdinanceType'
        type_label:
          type: string
          description: Human-readable ordinance type label
        person_id:
          type: string
          format: uuid
          description: Person ID (for individual ordinances BAPL, CONL, ENDL, SLGC)
        person_name:
          type: string
          description: Person's full name (denormalized for display)
        family_id:
          type: string
          format: uuid
          description: Family ID (for spouse sealing SLGS)
        date:
          $ref: '#/components/schemas/GenDate'
        place:
          type: string
          description: Location where ordinance was performed
        temple:
          type: string
          description: Temple code (GEDCOM TEMP tag)
        status:
          type: string
          description: Status (COMPLETED, BIC, CHILD, EXCLUDED, etc.)
        version:
          type: integer
          format: int64
        updated_at:
          type: string
          format: date-time

    LDSOrdinanceCreate:
      type: object
      required: [type]
      properties:
        type:
          $ref: '#/components/schemas/LDSOrdinanceType'
        person_id:
          type: string
          format: uuid
          description: Required for individual ordinances (BAPL, CONL, ENDL, SLGC)
        family_id:
          type: string
          format: uuid
          description: Required for spouse sealing (SLGS)
        date:
          type: string
          description: Date string in GEDCOM format
        place:
          type: string
        temple:
          type: string
          description: Temple code
        status:
          type: string
          description: Ordinance status

    LDSOrdinanceUpdate:
      type: object
      required: [version]
      properties:
        date:
          type: string
          description: Updated date
        place:
          type: string
          description: Updated place
        temple:
          type: string
          description: Updated temple code
        status:
          type: string
          description: Updated status
        version:
          type: integer
          format: int64
          description: Current version for optimistic locking

    LDSOrdinanceList:
      type: object
      required: [lds_ordinances, total]
      properties:
        lds_ordinances:
          type: array
          items:
            $ref: '#/components/schemas/LDSOrdinance'
        total:
          type: integer
          description: Total number of LDS ordinances
        limit:
          type: integer
        offset:
          type: integer

    # Export schemas for events and attributes
    EventExport:
      type: object
      description: Life event data for export
      required: [id, owner_type, owner_id, fact_type]
      properties:
        id:
          type: string
          format: uuid
        owner_type:
          type: string
          description: Either person or family
        owner_id:
          type: string
          format: uuid
        fact_type:
          type: string
          description: Type of event (BIRTH, DEATH, CENSUS, etc.)
        date:
          type: string
          description: Date in GEDCOM format
        place:
          type: string
        description:
          type: string
        cause:
          type: string
          description: Cause (for death events)
        age:
          type: string
          description: Age at event
        research_status:
          $ref: '#/components/schemas/ResearchStatus'
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time

    EventsExportResponse:
      type: object
      required: [events, total]
      properties:
        events:
          type: array
          items:
            $ref: '#/components/schemas/EventExport'
        total:
          type: integer

    AttributeExport:
      type: object
      description: Person attribute data for export
      required: [id, person_id, fact_type, value]
      properties:
        id:
          type: string
          format: uuid
        person_id:
          type: string
          format: uuid
        fact_type:
          type: string
          description: Type of attribute (OCCUPATION, RESIDENCE, etc.)
        value:
          type: string
        date:
          type: string
          description: Date in GEDCOM format
        place:
          type: string
        version:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time

    AttributesExportResponse:
      type: object
      required: [attributes, total]
      properties:
        attributes:
          type: array
          items:
            $ref: '#/components/schemas/AttributeExport'
        total:
          type: integer
